type createAccountResponse {
    ok: Boolean!
    error: String
    user: User
}

type Mutation {
    createAccount(
        username: String!
        email: String!
        firstName: String
        lastName: String
        bio: String
    ): createAccountResponse!
}

type Query {
    something: String!
}
